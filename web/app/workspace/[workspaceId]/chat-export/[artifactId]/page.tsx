"use client";import {useParams} from "next/navigation";import {useQuery} from "@tanstack/react-query";import {api} from "@/lib/api";import Shell from "@/components/workspace/shell";
export default function ChatExport(){const {workspaceId,artifactId}=useParams<{workspaceId:string,artifactId:string}>();const q=useQuery({queryKey:["artifact",artifactId],queryFn:()=>api<any>(`/api/artifacts/${artifactId}`)});return <Shell workspaceId={workspaceId}><h1 className="text-2xl font-semibold">{q.data?.name||"Imported conversation"}</h1><p className="muted mb-4">Read-only normalized conversation export · version {q.data?.version?.number}</p><article className="panel p-6 whitespace-pre-wrap leading-7 max-w-4xl">{q.data?.version?.text||"Loading artifact…"}</article></Shell>}
