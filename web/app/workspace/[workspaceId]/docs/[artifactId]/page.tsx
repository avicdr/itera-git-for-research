"use client";import {useParams} from "next/navigation";import {useQuery} from "@tanstack/react-query";import {api} from "@/lib/api";import Shell from "@/components/workspace/shell";import DocumentEditor from "@/components/editor/document-editor";
export default function Doc(){const {workspaceId,artifactId}=useParams<{workspaceId:string,artifactId:string}>();const q=useQuery({queryKey:["artifact",artifactId],queryFn:()=>api<any>(`/api/artifacts/${artifactId}`)});return <Shell workspaceId={workspaceId}><h1 className="text-2xl font-semibold">{q.data?.name||"Document"}</h1><p className="muted mb-4">Structured editor state · version {q.data?.version?.number}</p>{q.data&&<DocumentEditor artifactId={artifactId} initial={q.data.version.text}/>}</Shell>}
