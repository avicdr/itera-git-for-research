"use client";import {useParams} from "next/navigation";import {useQuery} from "@tanstack/react-query";import {api} from "@/lib/api";import Shell from "@/components/workspace/shell";export default function Commit(){const {workspaceId,commitId}=useParams<{workspaceId:string,commitId:string}>();const q=useQuery({queryKey:["commit",commitId],queryFn:()=>api<any>(`/api/commits/${commitId}`)});return <Shell workspaceId={workspaceId}><h1 className="text-2xl font-semibold">{q.data?.message||"Commit"}</h1><p className="muted">{q.data?.id}</p><div className="panel p-4 mt-5"><b>Parents</b><pre className="text-xs overflow-auto">{JSON.stringify(q.data?.parents,null,2)}</pre><b>Resulting artifact state</b><pre className="text-xs overflow-auto">{JSON.stringify(q.data?.state,null,2)}</pre></div></Shell>}
