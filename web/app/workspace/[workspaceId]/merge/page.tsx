"use client";import {useParams} from "next/navigation";import Shell from "@/components/workspace/shell";export default function Merge(){const {workspaceId}=useParams<{workspaceId:string}>();return <Shell workspaceId={workspaceId}><h1 className="text-2xl font-semibold">Merge</h1><div className="panel p-5 mt-5"><b>Merge branches with three-way conflict detection</b><p className="muted mt-2">Use the Branch comparison screen to inspect differences, then submit the merge through the API. Conflicts present both branch versions for Keep Main, Keep Branch, Combine, or manual resolution.</p></div></Shell>}
